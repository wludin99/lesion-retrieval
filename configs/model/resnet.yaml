# ResNet model configuration
name: resnet
backbone: microsoft/resnet-50  # HuggingFace ResNet (more MPS-compatible than timm)
pretrained: true
embedding_dim: 2048  # ResNet-50 feature dim

# Feature extraction
pool_type: avg  # avg, max, or adaptive
